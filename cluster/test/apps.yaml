---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: base-apps
  namespace: flux-system
spec:
  interval: 1m
  path: ./apps/
  prune: true
  sourceRef:
    kind: GitRepository
    name: github-repo
  timeout: 5m0s
  patches:
    - patch: |-
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: nginx-test
        spec:
          replicas: 1000
      target:
        kind: Deployment
        name: nginx-test